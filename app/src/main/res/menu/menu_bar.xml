<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@+id/home_actionbar"
        android:icon="@drawable/ic_action_home"
        android:visible="false"
        android:title="Home">
        </item>

    <item android:id="@+id/fav_actionbar"
        android:icon="@drawable/ic_action_fav"
        android:visible="false"
        android:title="Favorite">
         </item>

    <item android:id="@+id/share_actionbar"
        android:icon="@drawable/ic_action_share2"
        android:visible="false"
        android:title="Share with">
        </item>

    <item android:id="@+id/save_actionbar"
        android:icon="@drawable/ic_action_save"
        android:visible="false"
        android:title="Save As">
        </item>

</menu>